jdk:
  - openjdk21

before_install:
  - chmod +x gradlew

install:
  - ./gradlew :fabric:build

build:
  artifacts:
    # Publica todos los JARs generados dentro de fabric/build/libs
    - path: fabric/build/libs/*.jar
      type: jar
      name: identity-fabric
