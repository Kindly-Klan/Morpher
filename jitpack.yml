jdk:
  - openjdk21

before_install:
  - chmod +x gradlew

install:
  - ./gradlew :fabric:build

after_success:
  - mkdir -p build/libs
  - cp fabric/build/libs/*.jar build/libs/

build:
  artifacts:
    - path: build/libs/*.jar
      type: jar
      name: identity-fabric
